import React from 'react'
import useTitle from '../Hooks/useTitle'

const Collection = () => {
  
    useTitle("Collection")

  return (
    <div>
      collection
    </div>
  )
}

export default Collection
